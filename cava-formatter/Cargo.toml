[package]
name = "cava-formatter"
version = "0.1.0"
edition = "2024"

[dependencies]
# 二重起動防止・プロセス管理用
sysinfo = "0.30"
fd-lock = "4.0"

# 今後設定ファイルのパース等で使う場合は残す、使わなければ削除してもOKです
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
